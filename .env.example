# Copy this file to ".env" and add your real Anthropic API key.
# The server reads .env on startup. If no key is set, the server falls
# back to deterministic local heuristic analysis.

ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-6
